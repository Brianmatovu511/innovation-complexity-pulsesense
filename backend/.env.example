# Backend configuration
# Docker: keep HOST=0.0.0.0 so the container can accept connections.
HOST=0.0.0.0
PORT=8080

# Demo token for /ingest (optional).
# - If empty: ingest is OPEN (no auth)
# - If set: client must send header: Authorization: Bearer <token>
INGEST_TOKEN=dev-token-change-me

# Optional logging (nice for debugging)
RUST_LOG=info
